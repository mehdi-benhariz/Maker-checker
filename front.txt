+AdminPage (admin can add rules or change the rules)
-
+AddRequest(clien can submit request)
+AgentPage (can validate requests)

todo :- change the join in request history (add the progress and validation)
      -add order by in request history X
      -add 